query GetAllCountries($args: IndexCountryInput) {
  countries(indexCountryInput: $args) {
    id
    name
    nameEn
    slug
    alphaTwo
    iso
    isActive
    sort
    phonePrefix
    provincesCount
  }
}
