query GetCity($id: Int!) {
  city(id: $id) {
    name
    nameEn
    slug
    type
    sort
    isActive
    latitude
    longitude
    areas {
      id
      name
      nameEn
      slug
      sort
      isActive
    }
  }
}
