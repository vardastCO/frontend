query GetCity($id: Int, $slug: String) {
  city(id: $id, slug: $slug) {
    name
    nameEn
    slug
    type
    sort
    isActive
    areasCount
    latitude
    longitude
    areas {
      id
      name
      nameEn
      slug
      sort
      isActive
    }
  }
}
