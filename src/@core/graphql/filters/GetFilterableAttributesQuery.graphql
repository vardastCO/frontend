query GetFilterableAttributes($filterInput: FilterInput!) {
  filterableAttributes(filterInput: $filterInput) {
    filters {
      id
      name
      slug
      type
      isPublic
      isRequired
      isFilterable
      createdAt
      updatedAt
      values {
        options
        defaults
      }
    }
  }
}
