query GetCountry($id: Int, $slug: String) {
  country(id: $id, slug: $slug) {
    id
    name
    nameEn
    alphaTwo
    iso
    isActive
    provinces {
      id
      name
      nameEn
      isActive
      sort
      slug
    }
  }
}
