query GetBannerHomePage($IndexBannerInput: IndexBannerInput!) {
  getBannerHomePage(IndexBannerInput: $IndexBannerInput) {
    id
    originalName
    presignedUrl {
      url
      expiresAt
    }
  }
}
