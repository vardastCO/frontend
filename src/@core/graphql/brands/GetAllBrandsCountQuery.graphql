query GetAllBrandsCount($indexBrandInput: IndexBrandInput) {
  brands(indexBrandInput: $indexBrandInput) {
    total
    data {
      logoFile {
        id
        presignedUrl {
          url
        }
      }
    }
  }
}
