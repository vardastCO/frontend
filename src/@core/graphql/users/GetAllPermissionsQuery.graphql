query GetAllPermissions {
  permissions {
    total
    perPage
    currentPage
    lastPage
    to
    from
    data {
      id
      name
      displayName
      action
      subject
      field
      conditions
    }
  }
}
