mutation LoginUser($loginInput: LoginInput!) {
  login(loginInput: $loginInput) {
    accessToken
    accessTokenTtl
    user {
      id
    }
    abilities
    refreshToken
    refreshTokenTtl
  }
}
