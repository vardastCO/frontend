mutation CreateCity($createCityInput: CreateCityInput!) {
  createCity(createCityInput: $createCityInput) {
    id
    name
    nameEn
    slug
    type
    sort
    isActive
    latitude
    longitude
  }
}
