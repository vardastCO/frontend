mutation RemoveCountryMutation($id: Int!) {
  removeCountry(id: $id) {
    id
    name
    nameEn
    slug
    alphaTwo
    iso
    phonePrefix
    sort
    isActive
    latitude
    longitude
    flagEmoji
    provincesCount
  }
}
