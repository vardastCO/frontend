query GetAttribute($id: Int!, $slug: String) {
  attribute(id: 1, slug: null) {
    id
    name
    slug
    type
    createdAt
    updatedAt
    values {
      options
      defaults
    }
    isPublic
    isRequired
    isFilterable
  }
}
