mutation RemoveAttributeMutation($id: Int!) {
  removeAttribute(id: $id) {
    id
    name
    slug
    type
    createdAt
    updatedAt
    values {
      options
      defaults
    }
    isPublic
    isRequired
    isFilterable
  }
}
