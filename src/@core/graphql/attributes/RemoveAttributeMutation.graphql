mutation RemoveAttributeMutation($id: Int!) {
  removeAttribute(id: $id) {
    id
    name
    slug
    values {
      options
      defaults
      default
    }
    type
    createdAt
    updatedAt
  }
}
