query GetAllAttributes {
  attributes {
    total
    perPage
    currentPage
    lastPage
    to
    from
    data {
      id
      name
      slug
      type
      createdAt
      updatedAt
      values {
        options
        defaults
      }
      isPublic
      isRequired
      isFilterable
    }
  }
}
