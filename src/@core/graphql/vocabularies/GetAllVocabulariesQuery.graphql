query GetAllVocabularies {
  vocabularies {
    total
    perPage
    currentPage
    lastPage
    to
    from
    data {
      id
      title
      titleEn
      slug
      sort
      createdAt
      updatedAt
    }
  }
}
