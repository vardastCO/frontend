query GetCategoryBasics($id: Int, $slug: String) {
  category(id: $id, slug: $slug) {
    title
    titleEn
    slug
    icon
    sort
    isActive
    createdAt
    updatedAt
    childrenCount
    id
  }
}
